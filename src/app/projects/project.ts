/* Defines the product entity */
export interface IProject {
    projectId: number;
    projectName: string;
    releaseDate: string;
    coverage: number;
    description: string;
    starRating: number;
    imageUrl: string;
}

